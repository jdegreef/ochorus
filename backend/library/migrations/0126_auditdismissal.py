# Generated by Django 6.0.6 on 2026-09-07 14:22

from django.db import migrations, models


def enable_rls(apps, schema_editor):
    # Same rule as every other per-table RLS migration (e.g. 0107_article_rls):
    # the Supabase Data API exposes the public schema to the anon key, and RLS
    # with no policies denies all non-owner access. accounts/tests_rls enforces
    # "every public table has RLS"; Django connects as the owner and bypasses it,
    # so the app is unaffected. Postgres-only; SQLite dev has no RLS concept.
    if schema_editor.connection.vendor != "postgresql":
        return
    schema_editor.execute("ALTER TABLE library_auditdismissal ENABLE ROW LEVEL SECURITY")


def disable_rls(apps, schema_editor):
    if schema_editor.connection.vendor != "postgresql":
        return
    schema_editor.execute("ALTER TABLE library_auditdismissal DISABLE ROW LEVEL SECURITY")


class Migration(migrations.Migration):

    dependencies = [
        ('library', '0125_merge_two_0124_leaves'),
    ]

    operations = [
        migrations.CreateModel(
            name='AuditDismissal',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('check_key', models.CharField(max_length=40)),
                ('book', models.SlugField(max_length=200)),
                ('language', models.CharField(max_length=10)),
                ('ref', models.CharField(max_length=255)),
                ('note', models.TextField(blank=True)),
                ('reviewer', models.EmailField(blank=True, max_length=254)),
                ('created_at', models.DateTimeField(auto_now_add=True)),
            ],
            options={
                'ordering': ['-created_at'],
                'indexes': [models.Index(fields=['check_key'], name='library_aud_check_k_10da73_idx')],
                'constraints': [models.UniqueConstraint(fields=('check_key', 'book', 'language', 'ref'), name='uniq_audit_dismissal')],
            },
        ),
        migrations.RunPython(enable_rls, disable_rls),
    ]
