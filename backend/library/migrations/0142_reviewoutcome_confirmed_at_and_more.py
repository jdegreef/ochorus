# Generated by Django 6.0.6 on 2026-09-14 03:51

from django.db import migrations, models


def confirm_existing_approvals(apps, schema_editor):
    """Every pre-existing ``approved`` outcome was applied under the old rules
    (an approve always flipped source_type), so it is really CONFIRMED, not
    provisional. Stamp it so ``is_provisional`` reads true only for genuinely
    unconfirmed decisions going forward. No fixture backs this table, so on a
    fresh DB there are no rows and this no-ops."""
    ReviewOutcome = apps.get_model("library", "ReviewOutcome")
    ReviewOutcome.objects.filter(outcome="approved", confirmed_at__isnull=True).update(
        confirmed_at=models.F("decided_at"),
        confirmed_by=models.F("reviewer"),
    )


class Migration(migrations.Migration):

    dependencies = [
        ('library', '0141_alter_adminaction_action'),
    ]

    operations = [
        migrations.AddField(
            model_name='reviewoutcome',
            name='confirmed_at',
            field=models.DateTimeField(blank=True, null=True),
        ),
        migrations.AddField(
            model_name='reviewoutcome',
            name='confirmed_by',
            field=models.EmailField(blank=True, max_length=254),
        ),
        migrations.RunPython(confirm_existing_approvals, migrations.RunPython.noop),
    ]
