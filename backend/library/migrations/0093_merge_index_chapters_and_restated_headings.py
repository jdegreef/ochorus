"""Join the two 0092 leaves. No operations — this is graph repair only.

#1189 and #1190 merged within minutes of each other, each adding a migration
numbered 0092 and each green against a base that did not yet hold the other's:
`0092_strip_restated_chapter_headings` and `0092_drop_index_chapters`. The
result is two leaf nodes, which `makemigrations --check` rejects — so `main`
itself is red and every open pull request fails on it, whatever the PR changes.

A merge migration rather than a renumber, per `backend/CLAUDE.md`: both 0092s
have run against production, and renumbering one would rewrite history a
deployed database has already recorded. This depends on both and does nothing,
which is all the graph needs.

Generated by `manage.py makemigrations --merge` and renamed from its timestamp
so the file says what it joins.
"""

from django.db import migrations


class Migration(migrations.Migration):
    dependencies = [
        ("library", "0092_drop_index_chapters"),
        ("library", "0092_strip_restated_chapter_headings"),
    ]

    operations = []
