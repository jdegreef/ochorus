// Admin team / access console: authenticated, live data — SPA route, super-admin
// gated by the API (and hidden from the nav for non-super admins).
export const prerender = false;
export const ssr = false;
