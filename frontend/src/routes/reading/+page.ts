// /reading reads the reader's device-local progress from localStorage and
// resolves each work's cover/title from the public API — a personal, client-side
// SPA route (like /favorites and /notebook), never prerendered.
export const prerender = false;
export const ssr = false;
