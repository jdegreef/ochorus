// "My Library" reads the reader's device-local favorites from localStorage and
// resolves their covers/titles from the public API — a personal, client-side
// SPA route (like /notebook), never prerendered.
export const prerender = false;
export const ssr = false;
