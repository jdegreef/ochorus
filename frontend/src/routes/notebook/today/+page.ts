// The daily prayer is written into device-local storage (and synced from
// there) — keep it a client-side SPA route, like the Notebook itself.
export const prerender = false;
export const ssr = false;
