// The notebook reads device-local bookmarks/highlights from localStorage and
// resolves their text from the API — keep it a client-side SPA route.
export const prerender = false;
export const ssr = false;
